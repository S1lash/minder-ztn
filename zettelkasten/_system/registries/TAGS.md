# Tag Registry

All tags actually used in the system. Regenerated by
`_system/scripts/render_tags.py` — from `/ztn:maintain` (Step 7.10) and from
`regen_all.py`. Empty on a fresh instance; populates as `/ztn:process`
classifies new content. The tables between the markers below are never
hand-edited — the next run overwrites them. This preamble and `## Notes` are
owner-owned; the script never touches them.

> **This file = `tags:` axis only** — namespaced labels
> (`category/value`) in note frontmatter `tags:` lists. Not concepts
> (see `CONCEPT_NAMING.md`, snake_case identifiers in `concepts:`).
> Not audience tags (see `AUDIENCES.md`, privacy slots in
> `audience_tags:`). Three independent axes; do not conflate.

<!-- AUTO-GENERATED: tag-registry — DO NOT EDIT BETWEEN MARKERS (maintained by render_tags.py) -->
<!-- Source: `tags:` frontmatter across the knowledge layer (everything except `_records/`, `_sources/`, `_system/`, `0_constitution/`, `6_posts/`). Scope spec: the Notes section below. -->

**Total unique tags:** 0
**Total uses:** 0

---

_(no tags yet — populates as `/ztn:process` classifies content)_
<!-- END AUTO-GENERATED: tag-registry -->

---

## Notes

- Tags listed here are sourced from `tags:` lists in note frontmatter, knowledge layer only (excludes `_records/`, `_sources/`, `_system/`, `0_constitution/`, `6_posts/`) — plus two mechanical exclusions: `*.template.md` engine seeds and symlinks
- A tag repeated inside one note's `tags:` list counts once — a use is a note, not a mention
- Sections are namespaces, ordered by total uses; bare labels with no slash render last. Inside a section, tags are ordered by uses
- Convention drift: some tags use `cat/sub` and some use `cat-sub` — both are tolerated and counted as-is; the census surfaces the drift rather than merging it
- New tags should follow the dominant pattern in their category (look at existing usage)
