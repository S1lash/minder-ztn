PyYAML>=6.0
jsonschema>=4.0
pytest>=8.0
# Encrypts the roles/tools secrets blob (secret://…) — Fernet. Required only for a role
# that reaches an authenticated external system (the act path / a secret-bearing tool);
# lazy-imported, so a base install without acting roles never needs it at runtime.
cryptography>=41.0
